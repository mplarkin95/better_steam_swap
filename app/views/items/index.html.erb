<% if @items.present? %>
  <div class="row">
      <center>
      <%= render @items, locals: {item: @items} %>
      </center>
  </div>
  <%= will_paginate @items %>
<% else %> 
  <p> There are no items with <%= params[:search] %></p>
<% end %>
